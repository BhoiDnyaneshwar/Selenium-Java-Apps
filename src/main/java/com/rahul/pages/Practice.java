package com.rahul.pages;

import org.codehaus.plexus.util.Base64;

public class Practice {
	/*
	 * public static void main(String[] args) { String pass="Dnyanu@123"; byte[]
	 * encodePass=Base64.encodeBase64(pass.getBytes()); String passen1=new
	 * String(encodePass); //byte[]
	 * encodePass=Base64.getEncoder().encode(pass.getBytes()); //byte[]
	 * decodePass=Base64.getDecoder().decode(encodePass); String decodePass=new
	 * String(Base64.decodeBase64(encodePass));
	 * System.out.println(passen1+"  "+decodePass);
	 * 
	 * }
	 */

}
